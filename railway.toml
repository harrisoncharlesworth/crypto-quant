[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 -u scripts/run_live_bot.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production]
variables = {}
